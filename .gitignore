# Secrets — never commit real credentials
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Logs / local artifacts
*.log

# One-off local admin helper (not part of the deliverable)
reset_pg_password.ps1
reset_pg_log.txt
